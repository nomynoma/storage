
仕様書をChatGPTに作らせたやつ

# 成果物
https://codepen.io/nomynoma/full/JoorQRd


# 🧾 ピラミッド進捗可視化ツール仕様書

## 📌 概要
本ツールは、100段ピラミッドを構築するために必要な「ペンキ」（= ダイヤモンドブロック）を、ユーザーが現在どれだけ集めたかに応じて可視化するWebアプリケーションです。ピラミッドの上からまたは下から段階的に塗り進められる様子をアニメーションで表現します。

---

## 💡 目的
- 必要な資源量に対する現在の進捗確認
- 塗る順番（上から or 下から）の視覚的比較
- ゲームや作業のモチベーション支援

---

## ⚙️ 使用技術
- **HTML/CSS**：UI構築とスタイル
- **JavaScript**：ピラミッド構築・アニメーション処理・進捗計算
- **DOM API**：描画と入力処理

---

## 🧱 構成要素

### 🖥 HTML
- `input#diaCount`: ユーザーが所持しているダイヤ数（1〜356409）
- `input#diamondBlocks`: 現在のダイヤモンドブロック数（readonly）
- `div#pyramid`: ピラミッド本体の描画エリア
- 操作ボタン：
  - 「一番上から作成！」
  - 「一番下から作成！」
  - 「リセット」
  - 「キャンセル」

### 🎨 CSS
- ピラミッドの各ブロックサイズ：5px × 5px
- `block.visible`：塗られたブロックに対して `opacity: 1` を設定
- グラデーションとシャドウでダイヤ風の見た目

### 📜 JavaScript（主要関数）

#### `createPyramidFrame()`
- 100段のピラミッドをDOMに構築
- 各段に段数ラベルを追加（10段ごと）

#### `getPaintCostPerBlock(row)`
- 各段ごとの1マスあたりの塗装コストを返す
- 下段ほどコストが大きくなるロジック

#### `getTotalPaintNeeded()`
- 全100段を完全に塗るために必要なダイヤブロックの合計を算出（39601個）

#### `animateBlocks(direction)`
- ダイヤ数を元に塗れる範囲を決定し、順番にアニメーション表示
- `direction = "top"` or `"bottom"` で描画順を切り替え
- 各段ごとのコストに応じて減算処理

#### `clearBlocks()`
- 描画中のブロック表示とタイマーをリセット

---

## 📏 計算ロジック

- ユーザーが入力した **ダイヤ数 ÷ 9 = ペンキ個数（＝ダイヤモンドブロック）**
- 各段に必要な **ブロック数 × その段のペンキコスト** で、塗装可否を判断
- 全段塗るためには最大 **39601個のダイヤモンドブロック（＝356409個のダイヤ）** が必要

---

## ✅ 主な仕様と挙動

| 機能                | 内容                                                        |
|---------------------|-------------------------------------------------------------|
| ピラミッド表示       | 100段、段数に応じた幅で表示                                  |
| 塗装方向切替         | 上からまたは下から塗り始める処理                             |
| アニメーション       | 1ブロックずつ順に `fade-in` で表示                           |
| リアルタイム進捗表示 | 塗装率 (%) と現在のダイヤモンドブロック数を表示             |
| リセット・キャンセル | 状態初期化 or 現在の進行停止                                 |

---

## 🔧 拡張案（将来）

- 段ごとの詳細な消費量表示
- 目標設定機能（例：80段まで目指す）
- 保存・共有機能（進捗の記録）

